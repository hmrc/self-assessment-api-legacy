{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Business Address",
  "description": "Business Address",
  "type": "object",
  "properties": {
    "lineOne": {
      "title": "Business address line one",
      "description": "First line of the business address",
      "type": "string",
      "minLength": 1,
      "maxLength": 35,
      "pattern": "^[A-Za-z0-9 \\-,.&'\\/]{1,35}$"
    },
    "lineTwo": {
      "title": "Business address line two",
      "description": "Second line of the business address",
      "type": "string",
      "minLength": 1,
      "maxLength": 35,
      "pattern": "^[A-Za-z0-9 \\-,.&'\\/]{1,35}$"
    },
    "lineThree": {
      "title": "Business address line three",
      "description": "Third line of the business address",
      "type": "string",
      "minLength": 1,
      "maxLength": 35,
      "pattern": "^[A-Za-z0-9 \\-,.&'\\/]{1,35}$"
    },
    "lineFour": {
      "title": "Business address line four",
      "description": "Fourth line of the business address",
      "type": "string",
      "minLength": 1,
      "maxLength": 35,
      "pattern": "^[A-Za-z0-9 \\-,.&'\\/]{1,35}$"
    },
    "postcode": {
      "title": "Business postcode, mandatory when country is GB",
      "description": "Business postcode, mandatory when country is GB",
      "type": "string",
      "minLength": 1,
      "maxLength": 10,
      "pattern":"^[A-Z]{1,2}[0-9][0-9A-Z]?\\s?[0-9][A-Z]{2}|BFPO\\s?[0-9]{1,10}$"
    },
    "country": {
      "title": "Business country",
      "description": "Business country, ISO 3166-1 alpha-2",
      "type": "string",
      "minLength": 2,
      "maxLength": 2,
      "pattern":"^[A-Z]{2}$"
    }
  },
  "required": [
    "lineOne",
    "country"
  ],
  "additionalProperties": false
}
